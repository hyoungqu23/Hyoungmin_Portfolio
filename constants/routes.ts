interface Routes {
  HOME: string;
  ABOUT: string;
  PROJECTS: string;
  EXPERIENCE: string;
  MORE: string;
}

export const ROUTES: Routes = {
  HOME: '#home',
  ABOUT: '#about',
  PROJECTS: '#projects',
  EXPERIENCE: '#experience',
  MORE: '#more',
};

export default ROUTES;
