interface Routes {
  HOME: string;
  ABOUT: string;
  PROJECTS: string;
  EXPERIENCE: string;
  MORE: string;
}

const ROUTES: Routes = {
  HOME: '/',
  ABOUT: '/about',
  PROJECTS: '/projects',
  EXPERIENCE: '/experience',
  MORE: '/more',
};

export default ROUTES;
